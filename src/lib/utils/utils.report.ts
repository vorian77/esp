export enum RepElementType {
	analytic = 'analytic',
	column = 'column',
	group = 'group'
}

export enum RepParmType {
	boolean = 'boolean',
	date = 'date',
	link = 'link',
	number = 'number',
	string = 'string'
}
