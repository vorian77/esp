import { sectionHeader } from '$server/dbEdge/init/dbEdgeInitUtilities10'
import { addOrg } from '$server/dbEdge/init/dbEdgeInitUtilities50Other'

export async function initOrganizations() {
	sectionHeader('Organizations')
	// await addOrg({ name: 'app_cm' })
	// await addOrg({ name: 'app_cm_training' })
	// await addOrg({ name: 'app_db' })
	// await addOrg({ name: 'app_sys' })
	// await addOrg({ name: 'app_sys_rep' })
	// await addOrg({ name: 'app_sys_admin' })
	// await addOrg({ name: 'app_sys_admin_org' })
	// await addOrg({ name: 'app_sys_admin_user' })
}
